Feature: OPC

A short summary of the feature

@OPCpluginTesting
Scenario: For configure the OPC plugin
	Given At First Go to the admin page for OPC plugin
	When Click on the login Url for login as admin in OPC
	Then Give email and pass for admin login in kingdom for OPC
	Then Go to admin home before configure OPC items
	Then For configure OPC
